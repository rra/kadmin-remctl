#!/bin/sh
# /service/password-reset/run -- Run separate remctld for password reset.
# $Id$

exec env PATH="/usr/sbin:/usr/bin:/sbin:/bin" \
         KRB5_KTNAME=/etc/remctl/reset/keytab \
    tcpserver -H -R 0 4443 remctld -f /etc/remctl/reset/remctl.conf
